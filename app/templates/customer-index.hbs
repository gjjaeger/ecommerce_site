{{cust-navbar}}
<div class="cdisplay">
  <div class="product_display">
    {{#each model.products as |product|}}
      {{cproduct-tile product=product}}
    {{/each}}
  </div>

  {{display-cart product=product addsoldItems="addsoldItems"}}
</div>
