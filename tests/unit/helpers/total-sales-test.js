
import { totalSales } from 'e-commerce/helpers/total-sales';
import { module, test } from 'qunit';

module('Unit | Helper | total sales');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = totalSales([42]);
  assert.ok(result);
});

